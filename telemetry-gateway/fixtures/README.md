# Synthetic Fixtures

Every payload in this directory is synthetic and nonfunctional. Names such as `dev`, example absolute paths, reserved-looking identifiers, and token-shaped strings exist only to exercise adapter, policy, approval, and redaction behavior. They are not copied from a user system and contain no live credentials.
